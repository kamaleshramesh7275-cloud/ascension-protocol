{
    "version": 2,
    "functions": {
        "api/*.ts": {
            "maxDuration": 60,
            "includeFiles": "server/**,shared/**"
        }
    },
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index"
        }
    ]
}